version: '3.7'

services:
  coinpurse:
    build: .
    image: coinpurse:latest
    
    ports:
      - "5000:5000"

    volumes:      
      - data:/data 
    
    env_file: 
      - ./.env.dev

    command:
      flask run -h 0.0.0.0

volumes:
  data:
